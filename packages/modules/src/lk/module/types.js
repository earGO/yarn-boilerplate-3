import {lk as service} from '../../../import'

export const namespace = '@ursip-' + service.name

export const PRESELECT_TAB = `${namespace}/PRESELECT_TAB`

export const SELECT_PROJECT = `${namespace}/SELECT_PROJECT`
