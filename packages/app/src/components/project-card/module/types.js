import { name } from '../../../services/project-card'

export const namespace = '@ursip-' + name

export const EMPTY = `${namespace}/EMPTY`

export const LOAD_TABS = `${namespace}/LOAD_TABS`

export const LOAD_SECTIONS = `${namespace}/LOAD_SECTIONS`

export const LOAD_FIELDS = `${namespace}/LOAD_FIELDS`
